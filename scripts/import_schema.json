{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "updoc Bulk Import Schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "The title of the note."
      },
      "url": {
        "type": "string",
        "description": "The URL of the Google Doc."
      },
      "createdAt": {
        "type": "string",
        "format": "date-time",
        "description": "The creation date of the note in ISO 8601 format (e.g., '2026-04-20T17:40:00Z')."
      },
      "folderPath": {
        "type": "string",
        "description": "The path of the folder hierarchy, separated by forward slashes (e.g., 'Work/Project X'). Use an empty string or omit for the root folder."
      }
    },
    "required": ["title", "url", "createdAt", "folderPath"]
  }
}

